# Book Tracker

## 📂 Monorepo Structure

- **backend/** — Spring Boot service (Java + Hibernate)
- **frontend/** — React + TypeScript application
- **infra/** — Docker Compose, NGINX config, and infrastructure scripts
- **docs/** — non-code documentation (design, decisions, architecture notes)

Each folder has its own README with specific setup instructions.

## Quick start

Start Docker Compose to bring up Postgres + backend + frontend